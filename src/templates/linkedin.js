/** LinkedIn link/post image, 1200x627. */
import { makeLandscape } from './_landscape.js';

const t = makeLandscape({ id: 'linkedin', width: 1200, height: 627, label: 'LinkedIn post', density: 'tight' });
export const spec = t.spec;
export const render = t.render;
